# navigation
alias ll='ls -la'
alias dot='cd "$DOTFILES_DIR"'

# git
alias gs='git status'
alias gfs='xgfs'
alias gh='open "https://github.com/$(git config user.name)"'

# tmux
alias t='tmux'

# runtimes
alias j='jshell'

# sre
alias k='kubectl'
alias tf='terraform'

# editors
alias i='idea'
alias c='code'