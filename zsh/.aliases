# navigation
alias ll='ls -la'
alias dot='cd "$DOTFILES_DIR"'

# git
alias gs='git status'
alias gfs='xgfs'
alias gh='open "https://github.com/$(git config user.name)"'

# tmux
alias t='tmux'

# java
alias j='jshell'

# k8s
alias k='kubectl'